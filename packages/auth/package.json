{
  "name": "@repo/auth",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "better-auth": "^0.2.0",
    "@repo/database": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@repo/typescript-config": "workspace:*"
  }
}